"""
ASGI config for core project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application
from channels.routing import ProtocolTypeRouter, URLRouter
from channels.auth import AuthMiddlewareStack # supports authentication for WebSocket connections
import app.routing

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'core.settings')

application = ProtocolTypeRouter({
    "http": get_asgi_application(),
    "websocket": AuthMiddlewareStack(
        URLRouter(
            app.routing.websocket_urlpatterns
        )
    ),
})

# asgi.py configures the ASGI application for the Django project.
## It sets up the protocol type routing, including HTTP and WebSocket protocols.
## The AuthMiddlewareStack ensures that WebSocket connections support authentication.
## This setup allows the application to handle real-time WebSocket communication alongside traditional HTTP requests.


