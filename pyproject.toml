[project]
name = "silentcue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "axbouf", email = "axbouf@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "channels>=4.3.2",
    "channels-redis>=4.3.0",
    "daphne>=4.2.3",
    "django>=5.2.17",
]

[project.scripts]
silentcue = "silentcue:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
